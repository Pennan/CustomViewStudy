<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.np.a03graphics2d">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.demo1.BallMoveActivity" />
        <activity android:name=".view.demo2.CoordinateActivity" />
        <activity android:name=".view.demo3.ClipActivity" />
        <activity android:name=".view.demo3.ClipActivity2" />
        <activity android:name=".view.watch_view.WatchActivity"></activity>
    </application>

</manifest>